const positionExposureActionTypes = {
  USERS_FETCH_REQUESTED: 'USERS_FETCH_REQUESTED'
};

const fetchUsers = () => ({
  type: positionExposureActionTypes.USERS_FETCH_REQUESTED
});

export const positionExposureActions = {
  fetchUsers
};
